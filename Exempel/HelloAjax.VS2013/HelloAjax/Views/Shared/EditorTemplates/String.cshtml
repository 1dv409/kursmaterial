@Html.TextBox(String.Empty, ViewData.TemplateInfo.FormattedModelValue, 
    !String.IsNullOrWhiteSpace(ViewData.ModelMetadata.Watermark) ? 
    new { placeholder = ViewData.ModelMetadata.Watermark } : null)
